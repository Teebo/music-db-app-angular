import { FansCountPipe } from './fans-count.pipe';

describe('FansCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FansCountPipe();
    expect(pipe).toBeTruthy();
  });
});
